#Write a Python program that prints the calendar for a given month and year.Scripting Languages

import calendar

year = int(input("Input the year: "))
month = int(input("Inpu the month: "))

print(calendar.month(year,month))


